# Upstream provenance

This embedded Vue console was imported from
`RandyChen1985/yunshu-api-data-platform` at
`8c2ab44df8098e0f5993873defe7334f03b46792`.

It has been intentionally reduced to DB-GPT's unified data-governance UI. The
upstream SQL Lab, Text-to-SQL, LLM profiling, embedding, and vector-search code
are excluded. Authentication and API access use DB-GPT's existing JWT and
`/api/v1/serve/governance` endpoints.
